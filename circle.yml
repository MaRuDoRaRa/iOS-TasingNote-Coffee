 machine:
  timezone: Asia/Tokyo
  xcode:
    version: 8.3.3

dependencies:
  cache_directories:
    - "/Users/distiller/.cocoapods/"

test:
  override:
    - echo 'no test'

deployment:
  develop:
    branch: master
    commands:
      - bundle exec fastlane beta